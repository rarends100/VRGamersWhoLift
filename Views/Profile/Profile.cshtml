@*

*@
@model VRGamersWhoLift.Models.ViewModels.ProfileViewModel //pg 258 Murach
@{
    //Razor Syntax
    //Tag helpers to generate URLs pg 246
    //Access model props in a view pg 258

    ViewData["Title"] = "Profile Page";

}


<!DOCTYPE html>
<html>
    <head>
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Profile Page</title>
    </head>
    <body>
    <div>
        <img src="@Url.Content(Model.Picture)" alt="profile_photo" name="profile_photo" id="profile_photo" width="200" height="200" class="profile_photo_circle" />
    </div>
        <h1>Welcome @Model.UserName</h1>
        <p>Profile page</p>
        <div class="photo_chooser_box hidden" id="profile_photo_chooser">
            <partial name="~/Views/Shared/_uploadImage.cshtml" /> <!--https://learn.microsoft.com/en-us/aspnet/core/mvc/views/partial?view=aspnetcore-10.0-->
        <span style="color: red; margin-left: 100px; background-color: black;">@if (@Model.Errors != null && @Model.Errors.Count != 0 )
            { 
                foreach (string error in @Model.Errors) { @error; } 
            }</span>
    </div>
    </body>
</html>